# Copy to .env and edit. .env is gitignored.

# Initial password — only read on the first run.
SUPERVISOR_PASSWORD=changeme

# HTTP port.
SUPERVISOR_PORT=7778

# Bind address: 0.0.0.0 for LAN/Tailscale, 127.0.0.1 to lock to localhost.
SUPERVISOR_BIND=0.0.0.0
